generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Minimal starter schema. Extend/replace with your real domain models.
model Healthcheck {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}

